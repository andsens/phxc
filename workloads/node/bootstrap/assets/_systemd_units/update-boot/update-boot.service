[Unit]
Description=Update the boot partition with the latest image
Requires=boot.mount resource-ready@image-registry.service trust-smallstep-root.service
After=boot.mount resource-ready@image-registry.service clear-lease.service switch-boot.service trust-smallstep-root.service

[Install]
WantedBy=multi-user.target

[Service]
Type=oneshot
ExecStart=update-boot
