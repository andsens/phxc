[Unit]
Description=Report the final node-state to the boot-server
DefaultDependencies=no
Requires=systemd-timesyncd.service keys-available.target
After=systemd-timesyncd.service keys-available.target
Before=configure-networks.service

[Install]
WantedBy=sysinit.target

[Service]
Type=oneshot
RemainAfterExit=yes
ExecStart=report-node-state
