[Unit]
Description=Configures longhorn & snapshot integration
Requires=k8s-network-configured.target
After=k8s-network-configured.target

[Service]
Type=oneshot
RemainAfterExit=yes
ExecStart=manifest apply snapshots longhorn
ExecStartPost=resource-ready --wait longhorn
