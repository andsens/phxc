---
apiVersion: batch/v1
kind: Job
metadata:
  namespace: bootstrap
  name: build-k8s-master
  labels:
    app.kubernetes.io/name: bootstrap
    app.kubernetes.io/component: build-k8s-master
spec:
  template:
    metadata:
      labels:
        app.kubernetes.io/name: bootstrap
        app.kubernetes.io/component: build-k8s-master
        cluster.local/allow-internet-egress: "true"
    spec:
      restartPolicy: Never
      initContainers:
      - name: create-dockerfile
        image: distribution.docker-registry.svc.cluster.local/home-cluster-base:alpine
        imagePullPolicy: Always
        command: [/var/lib/home-cluster/workloads/bootstrap/commands/create-dockerfile.sh]
        args: [k8sMaster]
        volumeMounts:
        - name: home-cluster
          mountPath: /var/lib/home-cluster
          readOnly: true
        - name: images
          mountPath: /images/dockerfiles
          subPath: dockerfiles
      - name: kaniko
        image: gcr.io/kaniko-project/executor:latest
        args:
        - --build-arg
        - MACHINE=$(MACHINE)
        - --destination
        - distribution.docker-registry.svc.cluster.local/non-existent
        - --tar-path
        - /workspace/images/snapshots/$(MACHINE).tar
        - --no-push
        - --dockerfile
        - images/dockerfiles/$(MACHINE).Dockerfile
        env:
        - name: MACHINE
          value: k8sMaster
        volumeMounts:
        - name: home-cluster
          mountPath: /workspace
          readOnly: true
        - name: images
          mountPath: /workspace/images/dockerfiles
          subPath: dockerfiles
          readOnly: true
        - name: images
          mountPath: /workspace/images/snapshots
          subPath: snapshots
        - name: kaniko-docker-config
          mountPath: /kaniko/.docker
      containers:
      - name: create-uefi-boot-image
        image: distribution.docker-registry.svc.cluster.local/create-uefi-boot-image:latest
        imagePullPolicy: Always
        args: ["$(MACHINE)"]
        env:
        - name: MACHINE
          value: k8sMaster
        securityContext:
          privileged: true
        volumeMounts:
        - name: home-cluster
          mountPath: /var/lib/home-cluster
          readOnly: true
        - name: images
          mountPath: /workspace/images/snapshots
          subPath: snapshots
          readOnly: true
        - name: images
          mountPath: /workspace/images/raw
          subPath: raw
        - name: dev
          mountPath: /dev
      volumes:
      - name: home-cluster
        nfs:
          server: nfsShares.homeCluster.addr
          path: nfsShares.homeCluster.path
      - name: images
        nfs:
          server: nfsShares.images.addr
          path: nfsShares.images.path
      - name: kaniko-docker-config
        secret:
          secretName: kaniko-docker-config
      - name: dev
        hostPath:
          path: /dev
