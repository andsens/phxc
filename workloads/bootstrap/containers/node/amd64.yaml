---
apiVersion: batch/v1
kind: Job
metadata:
  name: build-amd64
  labels:
    app.kubernetes.io/name: bootstrap
    app.kubernetes.io/component: amd64
spec:
  template:
    metadata:
      labels:
        app.kubernetes.io/name: bootstrap
        app.kubernetes.io/component: amd64
        cluster.local/allow-internet-egress: "true"
    spec:
      restartPolicy: Never
      initContainers:
      - name: kaniko
        image: gcr.io/kaniko-project/executor:latest
        args:
        - --dockerfile=context/Dockerfile
        - --build-arg=ARCH=$(ARCH)
        - --destination=cluster.local/non-existent
        - --tar-path=/images/snapshots/$(ARCH).tar
        - --no-push
        env:
        - name: ARCH
          value: amd64
        volumeMounts:
        - name: context
          mountPath: /workspace/context
        - name: common-context
          mountPath: /workspace/common-context
        - name: bundle
          mountPath: /workspace/home-cluster.tar.gz
          subPath: home-cluster.tar.gz
        - name: images
          mountPath: /images/snapshots
          subPath: snapshots
      containers:
      - name: create-boot-images
        image: cluster.local/create-boot-images:latest
        imagePullPolicy: Never
        args: ["$(ARCH)"]
        env:
        - name: ARCH
          value: amd64
        volumeMounts:
        - name: scripts
          mountPath: /scripts
        - name: assets
          mountPath: /assets
        - name: node-settings
          mountPath: /node-settings
        - name: images
          mountPath: /images/snapshots
          subPath: snapshots
          readOnly: true
        - name: images
          mountPath: /images/pxe
          subPath: pxe
        - name: images
          mountPath: /images/uefi
          subPath: uefi
      volumes:
      - name: context
        configMap:
          name: node
      - name: scripts
        configMap:
          name: create-boot-images-scripts
          defaultMode: 0755
      - name: assets
        configMap:
          name: create-boot-images-assets
      - name: common-context
        configMap:
          name: common-context
          defaultMode: 0755
      - name: node-settings
        configMap:
          name: node-settings
      - name: bundle
        configMap:
          name: bundle
      - name: images
        persistentVolumeClaim:
          claimName: images
