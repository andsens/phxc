apiVersion: cluster.local
kind: BundleConfigMapGenerator
metadata:
  name: generate-bundle-cm
  annotations:
    config.kubernetes.io/function: |
      exec:
        path: ./scripts/generate-bundle-cm.sh
