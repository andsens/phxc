apiVersion: cluster.local
kind: ConfigMapFromDirGenerator
metadata:
  name: assets
  annotations:
    config.kubernetes.io/local-config: "true"
    config.kubernetes.io/function: |
      exec:
        path: ./cm-from-dir.sh
spec:
  name: assets
  path: assets
