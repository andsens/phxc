apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
generators:
- generate-cluster-vars-cm.yaml
patches:
- patch: |-
    apiVersion: v1
    kind: ConfigMap
    metadata: {name: cluster-vars, annotations: {config.kubernetes.io/local-config: "true"}}
