name: Upload the rpi-otp.yaml schema to gist.github.com

on:
  push:
    branches: [ 'master' ]
    paths:
    - .github/workflows/update-schema.yaml
    - workloads/node/bootstrap/assets/usr/local/lib/phoenix-cluster/rpi-otp.schema.yaml

jobs:
  update-rpi-otp-schema:
    name: Update the gist
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
      with:
        path: repo
    - name: Convert rpi-otp.schema.yaml to JSON
      run: yq -o=json . repo/workloads/node/bootstrap/assets/usr/local/lib/phoenix-cluster/rpi-otp.schema.yaml >rpi-otp.schema.json
    - name: Deploy to gist
      uses: exuanbo/actions-deploy-gist@v1
      with:
        token: ${{ secrets.GIST_TOKEN }}
        gist_id: ${{ vars.CONFIG_SCHEMAS_GIST_ID }}
        file_path: rpi-otp.schema.json
        file_type: application/schema+json
