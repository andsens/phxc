{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "$schema": {
      "type": "string"
    },
    "backup-server-addr": {
      "description": "The URL to the PHXC config backup server",
      "type": "string",
      "format": "url"
    },
    "kube-api-client-ca-addr": {
      "description": "The URL to the Kubernetes Client API CA smallstep server",
      "type": "string",
      "format": "url"
    },
    "ssh-keyname": {
      "description": "The name of the key loaded in ssh-agent to use for authentication",
      "type": "string"
    }
  },
  "required": [
    "backup-server-addr",
    "kube-api-client-ca-addr",
    "ssh-keyname"
  ],
  "additionalProperties": false
}
