[Unit]
Description=Mount the home-cluster share
After=network-online.target

[Install]
WantedBy=multi-user.target

[Mount]
What={%NFS_SHARES_HOME_CLUSTER_ADDR%}:{%NFS_SHARES_HOME_CLUSTER_PATH%}
Where=/var/lib/home-cluster
Type=nfs
