[Unit]
Description=Mount the persistent volume
Requires=mkfs-persistent.service
After=mkfs-persistent.service

[Install]
WantedBy=persistent-volume.target

[Mount]
What={%TRUENAS_PERSISTENT_VOLUME_DEV_PATH%}
Where=/var/lib/persistent
Type=ext4
Options=defaults
