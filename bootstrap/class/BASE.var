#!/usr/bin/env bash
# shellcheck disable=2034
FAI_RAMDISKS="${target:?}/var/lib/dpkg ${target:?}/var/cache ${target:?}/var/lib/apt/lists"
MACHINE_ID=$(get_setting "machines[\"$HOSTNAME\"].uuid")
FQDN=$(get_setting "machines[\"$HOSTNAME\"].hostname")
NFS_SHARES_HOME_CLUSTER_ADDR="$(get_setting nfsShares.homeCluster.addr)"
NFS_SHARES_HOME_CLUSTER_PATH="$(get_setting nfsShares.homeCluster.path)"
