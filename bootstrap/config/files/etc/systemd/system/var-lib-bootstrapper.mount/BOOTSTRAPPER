[Unit]
Description=Mount the bootstrapper share
After=network-online.target

[Install]
WantedBy=multi-user.target

[Mount]
What={%TRUENAS_HOST_BRIDGE_SERVER_IP%}:{%BOOTSTRAPPER_NFS_SHARE%}
Where=/var/lib/bootstrapper
Type=nfs
