[Unit]
Description=Bootstrap all images
Requires=var-lib-bootstrapper.mount
After=var-lib-bootstrapper.mount

[Install]
WantedBy=multi-user.target

[Service]
Type=oneshot
ExecStart=/usr/local/sbin/bootstrap
