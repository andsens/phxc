[Unit]
Description=Create /var/lib/persistent/images mountpoint
Requires=var-lib-persistent.mount
After=var-lib-persistent.mount
ConditionPathExists=!/var/lib/persistent/images

[Install]
WantedBy=persistent-volume.target

[Service]
Type=oneshot
ExecStart=mkdir /var/lib/persistent/images
