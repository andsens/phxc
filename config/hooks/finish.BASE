#!/usr/bin/env bash

error=0; trap 'error=$(($?>$error?$?:$error))' ERR # save maximum error code

skiptask chboot
# shellcheck source=../../lib/disk_var.sh
source "$LOGDIR/disk_var.sh"
export IMAGE_PATH
IMAGE_PATH=$(losetup -l "$BOOT_DEVICE" -OBACK-FILE | tail -n1)

exit $error
